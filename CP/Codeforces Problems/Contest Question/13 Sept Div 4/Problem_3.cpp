// C. Pacer-->>

#include <bits/stdc++.h>
using namespace std;

using ll = long long;
#define pb push_back
#define all(v) v.begin(), v.end()
#define sz(v) (int)v.size()
#define endl "\n"

const ll INF = 1e18;
const ll MOD = 1e9 + 7;

void solve() {
    ll n,m;
    cin >> n >> m;
    vector<vector<ll>> v(n,vecto<ll>(2));
    for(int i = 0;i<n;i++){
        for(int j = 0;j<2;j++){
            cin >> a >> v[i][j];
        }
    }
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t = 1;
    cin >> t;   // if multiple test cases
    while(t--) {
        solve();
    }
}
